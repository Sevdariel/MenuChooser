services:
  webapi:
    build:
      dockerfile: Dockerfile
    ports:
      - "7103:80"
    environment:
      ASPNETCORE_ENVIRONMENT: "Development"
      DatabaseConfiguration__ConnectionString: "mongodb://mongo:27017"
    volumes:
      - ./MenuChooser-API:/app
    command: ["dotnet", "watch", "run", "--urls", "http://0.0.0.0:7103"]

  mongo:
    ports:
      - "27018:27017"
